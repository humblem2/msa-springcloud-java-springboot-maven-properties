# Logback logging(for 'LOG_FILE_IS_UNDEFINED' log file is NOT being created on application startup in AWS Env[beta, real])
logging.file=/home/ubuntu/app/git/00.logs/${spring.application.name}/beta/app-log-init-time/${spring.application.name}
